<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Focused state (blue) -->
    <item android:color="@color/color_stroke_focused" android:state_focused="true"/>
    <!-- Default state (gray) -->
    <item android:color="@color/color_stroke_default"/>
</selector>